name: datagrid
description: YAML configuratable responsive datagrid.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.6 <3.0.0"

dependencies:
  dio: ^4.0.6
  flutter:
    sdk: flutter
  intl: ^0.18.0
  yaml: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true

  assets:
    - datagrid.yaml
